% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ZI_MLN.R
\name{ZI_MLN}
\alias{ZI_MLN}
\title{Fit the Zero-Inflated Multivariate Rounded Log-Normal (ZI-MLN) model}
\usage{
ZI_MLN(
  Y,
  X = NULL,
  m = NULL,
  M = NULL,
  K = 10,
  niter = 20000,
  burnin = floor(niter/2),
  seed = 3,
  a.phi = 1/2,
  a.tau = 1,
  b.tau = 1/50,
  a.sig = 3,
  b.sig = 3,
  a.vs2 = 3,
  b.vs2 = 3,
  acc.target = 0.44,
  Lr = 8,
  a.phi.r = 3,
  a.w = 5,
  b.w = 5,
  ur2 = 1,
  L.theta = 15,
  a.phi.theta = 1,
  a.w.theta = 5,
  b.w.theta = 5,
  u2.theta = 1,
  m_kappa = 0,
  sig_kappa = 3,
  m_beta = 0,
  sig_beta = 25
)
}
\arguments{
\item{Y}{Integer count matrix, \code{n} samples (rows) by \code{J} OTUs (columns). Raw
counts; no normalization is required. Zeros are allowed and modelled.}

\item{X}{Optional numeric covariate matrix, \code{n} by \code{P}, one row per sample.
Used both in the mean model for abundance and (with an added intercept) in
the probit model for the zero-inflation probability. \code{NULL} (default) fits
the no-covariate model.}

\item{m}{Integer vector of length \code{n} giving the subject/group index of each
sample. For example \code{c(1, 1, 2, 3)} means samples 1-2 come from subject 1,
sample 3 from subject 2 and sample 4 from subject 3. Defaults to
\code{seq_len(nrow(Y))} (each sample is its own subject).}

\item{M}{Number of distinct subjects/groups. Defaults to
\code{length(unique(m))}.}

\item{K}{Factor dimension of the low-rank covariance decomposition
\eqn{\Sigma = \Lambda\Lambda' + \sigma^2 I}. Fixed at a moderately large
value (default 10).}

\item{niter}{Total number of MCMC iterations (default 20000).}

\item{burnin}{Number of initial iterations discarded as burn-in. Draws are
stored for iterations \verb{> burnin}. Defaults to \code{floor(niter / 2)}.}

\item{seed}{Random seed for reproducibility (default 3).}

\item{a.phi}{Dirichlet-Laplace concentration \eqn{a_\phi} controlling
sparsity of the covariance (default 1/2). Smaller values shrink more.}

\item{a.tau, b.tau}{Gamma shape/rate hyperparameters for \eqn{\tau_k}
(defaults 1 and 1/50).}

\item{a.sig, b.sig}{Inverse-gamma hyperparameters for the idiosyncratic
variance \eqn{\sigma^2} (defaults 3, 3).}

\item{a.vs2, b.vs2}{Inverse-gamma hyperparameters for the subject random-effect
variance \eqn{u_s^2} (defaults 3, 3).}

\item{acc.target}{Target acceptance rate for the adaptive Metropolis update of
\eqn{\phi_j} (default 0.44).}

\item{Lr, a.phi.r, a.w, b.w, ur2}{Hyperparameters of the mean-constrained
mixture-of-mixtures prior on the sample-size factors \eqn{r_i}: number of
mixture components, Dirichlet concentration, two Beta parameters and the
component variance.}

\item{L.theta, a.phi.theta, a.w.theta, b.w.theta, u2.theta}{Corresponding
hyperparameters of the mixture-of-mixtures prior on the OTU-size factors
\eqn{\alpha_j}.}

\item{m_kappa, sig_kappa}{Prior mean and variance for the probit
zero-inflation coefficients \eqn{\kappa} (defaults 0, 3).}

\item{m_beta, sig_beta}{Prior mean and variance for the abundance regression
coefficients \eqn{\beta} (defaults 0, 25). Ignored when \code{X = NULL}.}
}
\value{
A list of length \code{niter - burnin}. Each element is one posterior draw,
itself a list with components:
\describe{
\item{\code{Lambda}}{\code{J} by \code{K} factor loading matrix.}
\item{\code{sig2}}{idiosyncratic variance \eqn{\sigma^2}.}
\item{\code{vs2}}{subject random-effect variance \eqn{u_s^2}.}
\item{\code{phi},\code{tau.k}}{Dirichlet-Laplace parameters.}
\item{\code{eta}}{\code{n} by \code{K} latent factor scores.}
\item{\code{ri},\code{thetaj}}{sample-size factors \eqn{r_i} and OTU-size factors
\eqn{\alpha_j} (only \eqn{r_i + \alpha_j} is identifiable).}
\item{\code{sij},\code{smj}}{sample- and subject-level random effects.}
\item{\code{beta}}{\code{J} by \code{P} abundance regression coefficients (present only
when \code{X} is supplied).}
\item{\code{delta}}{\code{n} by \code{J} presence indicators; \code{1} = OTU present (drawn
from the count distribution), \code{0} = structural/absent zero. (Note this
is the complement of the \eqn{\delta_{ij}} in the paper.)}
\item{\code{eps.ij}}{\code{n} by \code{J} matrix of estimated absence probabilities
\eqn{\epsilon_{ij}} (the paper's zero-inflation probability).}
\item{\code{kappa}}{\code{J} by \code{P + 1} probit zero-inflation coefficients
(intercept in the first column).}
}

Common posterior summaries: the marginal OTU correlation matrix is
\code{cov2cor(tcrossprod(draw$Lambda) + diag(draw$vs2 + draw$sig2, J))}, averaged
over draws.
}
\description{
Runs the Markov chain Monte Carlo (MCMC) sampler for the ZI-MLN model of
Zhang et al. (2023). The model infers the interaction (covariance) structure
between microbial features (OTUs) directly from an observed count table,
accommodating overdispersion, compositionality, subject-level random effects,
covariate effects on abundance, and excess (structural) zeros.
}
\details{
A single, unified interface handles both the no-covariate and the
with-covariate cases: when \code{X = NULL} the covariate regression terms are
dropped and the zero-inflation component reduces to an intercept-only probit
model, reproducing the "without covariate" sampler.
}
\examples{
\donttest{
sim <- simulate_zimln(n = 30, J = 30, K = 3, seed = 1)
fit <- ZI_MLN(sim$Y, m = sim$m, M = sim$M, niter = 400, burnin = 200)
# posterior mean marginal correlation
J <- ncol(sim$Y)
rho <- Reduce(`+`, lapply(fit, function(d)
  cov2cor(tcrossprod(d$Lambda) + diag(d$vs2 + d$sig2, J)))) / length(fit)
}

}
\references{
Zhang, S., Shuler, K., Lee, J. and Jeganathan, P. (2023). Bayesian Modeling of
Interaction between Features in Sparse Multivariate Count Data with Application
to Microbiome Study. \emph{Annals of Applied Statistics}, 17(3).
\doi{10.1214/22-AOAS1690}
}
