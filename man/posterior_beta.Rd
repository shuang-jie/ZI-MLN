% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_summaries.R
\name{posterior_beta}
\alias{posterior_beta}
\title{Posterior summary of the abundance regression coefficients beta}
\usage{
posterior_beta(fit, prob = c(0.025, 0.975))
}
\arguments{
\item{fit}{A list returned by \code{\link[=ZI_MLN]{ZI_MLN()}} with a \code{beta} component in each draw.}

\item{prob}{Length-2 vector of lower/upper probabilities for the credible
interval. Default \code{c(0.025, 0.975)}.}
}
\value{
A list with three \verb{J x P} matrices: \code{mean}, \code{lower} and \code{upper}
(posterior mean and credible bounds of each \eqn{\beta_{jp}}), plus a tidy
long \code{data.frame} \code{table} with columns \code{otu}, \code{covariate}, \code{mean}, \code{lower},
\code{upper}.
}
\description{
Summarises the posterior of the covariate effects \eqn{\beta_{jp}} from a
\code{\link[=ZI_MLN]{ZI_MLN()}} fit that was run \emph{with} covariates. Errors if the model was fit
without covariates (\code{X = NULL}).
}
\examples{
\donttest{
sim <- simulate_zimln(n = 30, J = 20, K = 2, p = 2, seed = 1)
fit <- ZI_MLN(sim$Y, X = sim$X, m = sim$m, M = sim$M, niter = 400, burnin = 200)
b <- posterior_beta(fit)
head(b$table)
}
}
\seealso{
\code{\link[=posterior_correlation]{posterior_correlation()}}, \code{\link[=posterior_covariance]{posterior_covariance()}}
}
