% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_summaries.R
\name{posterior_correlation}
\alias{posterior_correlation}
\title{Posterior marginal correlation between OTUs}
\usage{
posterior_correlation(fit, ci = FALSE, prob = c(0.025, 0.975))
}
\arguments{
\item{fit}{A list returned by \code{\link[=ZI_MLN]{ZI_MLN()}} (one element per posterior draw).}

\item{ci}{Logical; if \code{TRUE} also return element-wise credible bounds. This
builds a \verb{J x J x ndraw} array and can use substantial memory for large \code{J}.
Default \code{FALSE} (posterior mean only).}

\item{prob}{Length-2 vector of lower/upper probabilities for the credible
interval when \code{ci = TRUE}. Default \code{c(0.025, 0.975)}.}
}
\value{
If \code{ci = FALSE}, the \verb{J x J} posterior-mean correlation matrix. If
\code{ci = TRUE}, a list with \code{mean}, \code{lower} and \code{upper} \verb{J x J} matrices.
}
\description{
Summarises the posterior of the marginal correlation matrix
\eqn{\rho_{jj'}} implied by a \code{\link[=ZI_MLN]{ZI_MLN()}} fit. For each retained draw the
marginal correlation is computed from the covariance
\eqn{\Omega = \Lambda\Lambda' + (\sigma^2 + u_s^2) I} as
\code{cov2cor(tcrossprod(Lambda) + diag(vs2 + sig2, J))}, and the draws are then
summarised element-wise.
}
\examples{
\donttest{
sim <- simulate_zimln(n = 20, J = 20, K = 2, seed = 1)
fit <- ZI_MLN(sim$Y, m = sim$m, M = sim$M, niter = 400, burnin = 200)
rho <- posterior_correlation(fit)
}
}
\seealso{
\code{\link[=posterior_covariance]{posterior_covariance()}}, \code{\link[=posterior_beta]{posterior_beta()}}
}
