% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_summaries.R
\name{posterior_covariance}
\alias{posterior_covariance}
\title{Posterior covariance between OTUs}
\usage{
posterior_covariance(fit, marginal = TRUE, ci = FALSE, prob = c(0.025, 0.975))
}
\arguments{
\item{fit}{A list returned by \code{\link[=ZI_MLN]{ZI_MLN()}}.}

\item{marginal}{Logical; add the subject random-effect variance \eqn{u_s^2} to
the diagonal (\code{TRUE}, default, giving \eqn{\Omega}) or not (\code{FALSE}, giving
\eqn{\Sigma}).}

\item{ci, prob}{As in \code{\link[=posterior_correlation]{posterior_correlation()}}.}
}
\value{
A \verb{J x J} posterior-mean covariance matrix, or a list of
\code{mean}/\code{lower}/\code{upper} matrices when \code{ci = TRUE}.
}
\description{
Summarises the posterior of the OTU covariance matrix from a \code{\link[=ZI_MLN]{ZI_MLN()}} fit.
By default the \emph{marginal} covariance
\eqn{\Omega = \Lambda\Lambda' + (\sigma^2 + u_s^2) I} is returned (the one
whose correlation is \code{\link[=posterior_correlation]{posterior_correlation()}}). Set \code{marginal = FALSE} for the
interaction covariance \eqn{\Sigma = \Lambda\Lambda' + \sigma^2 I}, which
excludes the subject random-effect variance.
}
\examples{
\donttest{
sim <- simulate_zimln(n = 20, J = 20, K = 2, seed = 1)
fit <- ZI_MLN(sim$Y, m = sim$m, M = sim$M, niter = 400, burnin = 200)
Sigma <- posterior_covariance(fit, marginal = FALSE)
}
}
\seealso{
\code{\link[=posterior_correlation]{posterior_correlation()}}, \code{\link[=posterior_beta]{posterior_beta()}}
}
