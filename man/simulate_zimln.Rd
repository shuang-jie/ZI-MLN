% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_zimln.R
\name{simulate_zimln}
\alias{simulate_zimln}
\title{Simulate a count table from the ZI-MLN generative model}
\usage{
simulate_zimln(
  n = 20,
  J = 150,
  K = 5,
  zero.rate = 0.8,
  sig2 = 1,
  vs2 = 1,
  p = 0,
  M = n,
  m = NULL,
  seed = 1
)
}
\arguments{
\item{n}{Number of samples.}

\item{J}{Number of OTUs (features).}

\item{K}{Number of latent factors used to build the true covariance.}

\item{zero.rate}{Proportion of entries in each column of the true loading
matrix \code{Lambda} set to zero (controls covariance sparsity). Default 0.8.}

\item{sig2}{True idiosyncratic variance \eqn{\sigma^2} (default 1).}

\item{vs2}{True subject random-effect variance \eqn{u_s^2} (default 1).}

\item{p}{Number of continuous covariates to generate. \code{0} (default) produces a
no-covariate data set (\code{X} is \code{NULL}).}

\item{M}{Number of subjects/groups (default \code{n}, i.e. one sample per subject).}

\item{m}{Optional length-\code{n} subject index. Defaults to a balanced assignment
of the \code{n} samples to the \code{M} subjects.}

\item{seed}{Random seed (default 1).}
}
\value{
A list with the simulated data and the ground truth:
\describe{
\item{\code{Y}}{\code{n} by \code{J} integer count matrix (the model input).}
\item{\code{X}}{\code{n} by \code{p} covariate matrix, or \code{NULL} when \code{p = 0}.}
\item{\code{m},\code{M}}{subject index and subject count.}
\item{\code{Lambda},\code{sig2},\code{vs2}}{true covariance components.}
\item{\code{Omega},\code{true.cor}}{true covariance \verb{Lambda Lambda' + sig2 I} and the
true marginal correlation matrix \verb{cov2cor(Omega + vs2 I)}.}
\item{\code{ri},\code{thetaj},\code{smj}}{true sample-size / OTU-size factors and subject
random effects.}
\item{\code{beta}}{true \code{J} by \code{p} regression coefficients (\code{NULL} when \code{p = 0}).}
\item{\code{kappa}}{true \code{J} by \code{p + 1} probit zero-inflation coefficients.}
\item{\code{eps}}{\code{n} by \code{J} true absence probabilities \eqn{\epsilon_{ij}}.}
\item{\code{delta}}{\code{n} by \code{J} true presence indicators (\code{1} = present).}
}
}
\description{
Draws a synthetic OTU count matrix (and, optionally, covariates) directly from
the zero-inflated multivariate rounded log-normal model, together with the
true parameter values used to generate it. Useful for examples, unit tests and
checking parameter recovery with \code{\link[=ZI_MLN]{ZI_MLN()}}.
}
\examples{
sim <- simulate_zimln(n = 20, J = 30, K = 3, seed = 1)
dim(sim$Y)
mean(sim$Y == 0)   # overall zero rate

}
\seealso{
\code{\link[=ZI_MLN]{ZI_MLN()}}
}
